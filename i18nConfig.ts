const i18nConfig = {
    locales: ['en', 'pt-BR', 'es'],
    defaultLocale: 'pt-BR',
    prefixDefault: true,
    serverSetCookie: 'always' as 'always' | 'if-empty' | 'never'
};

export default i18nConfig;
